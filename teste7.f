programa repeticao_simples;
var i, total: inteiro;
inicio
    i := 1;
    Escrever(i);
    total := 0;
    Escrever(total);
    enquanto i <= 5 faca
        total := total + i;
        i := i + 1;
    fim;
    Escrever("Total:", total);
fim.