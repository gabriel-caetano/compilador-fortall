/* Exemplo de programa aceito pela gramática */
programa t;
var x: logico;
var a, b: inteiro;
inicio
    a := 10;
    b := 11;
    x := a <= b;
    Escrever("asdf", x, x, x, "aaa", 15, "sfb", x);
fim.
/* Fim do programa */
